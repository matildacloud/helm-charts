apiVersion: v1
kind: Service
metadata:
  name: k8s-agent
  labels:
    app: k8s-agent 
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: k8s-agent 
